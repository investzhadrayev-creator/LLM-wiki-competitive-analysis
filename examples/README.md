# examples/ — эталонные файлы

Эти файлы — образцы формата (валидная карточка, candidate, derived-блок отчёта).
Тесты (tests/run_tests.sh) копируют их во временные wiki/ и outputs/ при прогоне.
В рабочей базе examples/ не участвует.
